fn main() {
    let is_username_correct: bool = true;
    let is_password_correct: bool = false;

    if is_username_correct && is_password_correct {
        println!("Login successful");
    } else {
        println!("Login failed");
    }
}
